#include <stdio.h>

int main()
{
   int i=2,j;
   printf("the prime numbers are:\n");
   printf("\t%d",i);
   j++;
  jumphere:
   if(j<=100)
   {
       if(j<i)
         if(i%j==0)
       
       if(j==i)
       goto jumphere;
       
   }
}
