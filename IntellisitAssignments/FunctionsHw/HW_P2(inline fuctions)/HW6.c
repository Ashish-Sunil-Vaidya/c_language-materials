
//	WAP to check whether the given integer is palindrome or not.
#include<stdio.h>
int palindrome(int num)
{   
   
    int reverse,dummynum,remainder,temp=0;
    dummynum = num;
   
    return reverse;
}

void main()
{   
    int num;
    printf("Enter a integer: ");
    scanf("%d",&num);
    

    if(num==palindrome(num)) printf("The number %d is a palindrome",num);
    else printf("The number %d is not a palindrome",num);
}